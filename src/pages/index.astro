---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { TAG_LABELS } from '../tags';

const posts = (await getCollection('blog'))
	.filter((post) => post.data.lang === 'en')
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const featuredProjects = [
	{
		id: 'tinboker',
		title: 'TinBoker — AI Audio & Podcast Knowledge Platform',
		tagline: 'An AI-native content platform combining unstructured speech processing, LangGraph multi-agent DAGs, and tiered caching.',
		tags: ['React 19', 'TypeScript', 'Vite', 'FastAPI', 'Python 3.12', 'LangGraph', 'Redis', 'Docker', 'Caddy'],
		highlights: [
			'Multi-Agent Topology: Filters ad noise, dynamically consolidates chapters by length, and extracts key insights.',
			'Step-by-step Checkpoints: Stores Raw / Checkpoint / Serving data layers for rerun_from failure recovery.',
			'Tiered Caching: FastAPI + Redis cache-aside with Single-Flight locks to prevent cache stampedes.'
		],
		links: [
			{ label: 'Visit Website', href: 'https://tinboker.com', external: true },
			{ label: 'GitHub Repo', href: 'https://github.com/HaoweiChan/tinboker', external: true },
			{ label: 'Architecture Post (Part 1)', href: '/blog/agentic-ingestion-pipeline-part1/' },
			{ label: 'Production Post (Part 2)', href: '/blog/agentic-ingestion-pipeline-part2/' }
		]
	},
	{
		id: 'kuroshio',
		title: 'Kuroshio — Open-Source Quantamental Portfolio Engine',
		tagline: 'Multi-factor screening, LLM agent research, and IPS-driven rebalancing proposals. It ranks, argues, and proposes — it never places an order.',
		tags: ['Python 3.11+', 'Multi-Agent Research', 'IPS Policy Engine', 'Backtest', 'Apache-2.0'],
		highlights: [
			'Screen: multi-factor momentum screeners for US and TW equities, scored by cross-sectional percentile so both markets sit on one scale.',
			'Research: one LLM agent pass per holding, facet-cached so a daily run costs cents rather than dollars.',
			'Propose: challenger-vs-weakest-incumbent swap cards, each citing the IPS clause that triggered it — resource competition instead of a binary veto.'
		],
		links: [
			{ label: 'GitHub Repo', href: 'https://github.com/HaoweiChan/kuroshio', external: true },
			{ label: 'Live Demo', href: 'https://haoweichan.github.io/kuroshio/', external: true }
		]
	},
	{
		id: 'hermes',
		title: 'Hermes Agent — Resident Personal AI Assistant System',
		tagline: 'Dual-entry resident assistant with Discord gateway, cron scheduler, plain-text vault memory, and automated portfolio tracking.',
		tags: ['Python 3.11', 'Discord Gateway', 'Cron Scheduler', 'Obsidian Vault', 'Git Sweeper', 'Shioaji API', 'Schwab API'],
		highlights: [
			'Channels as Roles: Configures Discord channels as Kuroshio desk (#trading), report feeds (#reports), and research (#research).',
			'Automated Portfolio Engine: Daily encrypted PDF statement parsing, Schwab/Shioaji API integration, T+2 settlement calculations, and auto-charting.',
			'Single Git Sweeper: Decouples file writing from Git operations with scheduled automated commits & rebases.'
		],
		links: [
			{ label: 'hermes-desk (scheduling layer)', href: 'https://github.com/HaoweiChan/hermes-desk', external: true },
			{ label: 'Assistant Architecture Post', href: '/blog/resident-personal-assistant/' },
			{ label: 'Portfolio Automation Post', href: '/blog/broker-reports-and-portfolio-automation/' },
			{ label: 'Hermes Agent (upstream)', href: 'https://github.com/NousResearch/hermes-agent', external: true }
		]
	},
	{
		id: 'taifexdesk',
		title: 'TaifexDesk — TAIFEX Futures & Options Trading Infrastructure',
		tagline:
			'Backtesting, paper trading, and live execution on one engine for Taiwan index futures, single-stock futures, and TXO options — Python + FastAPI backend, React war-room dashboard, shioaji broker adapter.',
		tags: ['Python', 'FastAPI', 'React', 'Shioaji', 'Backtesting', 'Paper Trading', 'Risk Engine'],
		highlights: [
			'Signals separated from execution: a strategy is just an entry / add / stop policy triple, so the engine ships open-source while the strategies stay private.',
			'Intra-bar stop checking: reconstructs a plausible price path from OHLC, so a bar whose low pierced the stop counts as stopped out — removing the systematic optimism of close-only backtests.',
			'One cost model everywhere: backtest, paper, and live share the same commission and tax math, so results do not drift apart as the index moves.'
		],
		links: [{ label: 'GitHub Repo', href: 'https://github.com/HaoweiChan/TaifexDesk', external: true }]
	}
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header switchHref="/zh-tw/" />
		<main class="home">
			<section class="intro" aria-labelledby="intro-title">
				<p class="eyebrow">Portfolio and notes</p>
				<h1 id="intro-title">Haowei Chan</h1>
				<p class="lede">
					I build web products and AI systems, document engineering decisions, and keep a public trail of projects,
					technical notes, and experiments.
				</p>
				<div class="actions" aria-label="Primary links">
					<a class="button" href="/blog">Read all articles</a>
					<a class="button secondary" href="https://github.com/HaoweiChan" target="_blank" rel="noreferrer"
						>View GitHub</a
					>
				</div>
			</section>

			<section class="section" aria-labelledby="projects-title">
				<div class="section-heading">
					<p class="eyebrow">Featured</p>
					<h2 id="projects-title">Projects</h2>
				</div>

				<div class="expandable-grid">
					{
						featuredProjects.map((project) => (
							<details class="expand-card project-card" id={`proj-${project.id}`}>
								<summary class="expand-summary">
									<div class="summary-header">
										<h3 class="card-title">{project.title}</h3>
										<span class="chevron" aria-hidden="true" />
									</div>
									<p class="card-tagline">{project.tagline}</p>
									<div class="tag-row">
										{project.tags.map((tag) => (
											<span class="tech-tag">{tag}</span>
										))}
										{project.private && <span class="tech-tag private-tag">{project.private}</span>}
									</div>
								</summary>
								<div class="expand-content">
									<div class="content-block">
										<h4>Architecture & Key Features</h4>
										<ul>
											{project.highlights.map((item) => (
												<li>{item}</li>
											))}
										</ul>
									</div>
									<div class="card-actions" hidden={project.links.length === 0}>
										{project.links.map((link) => (
											<a
												class="action-btn"
												href={link.href}
												target={link.external ? '_blank' : '_self'}
												rel={link.external ? 'noreferrer' : ''}
											>
												{link.label}
											</a>
										))}
									</div>
								</div>
							</details>
						))
					}
				</div>
			</section>

			<section class="section" aria-labelledby="writing-title">
				<div class="section-heading">
					<p class="eyebrow">Latest</p>
					<h2 id="writing-title">Writing</h2>
				</div>

				<div class="post-list">
					{
						posts.map((post) => (
							<a class="post-row" href={`/blog/${post.id}/`}>
								<div class="post-text">
									<h3 class="post-title">{post.data.title}</h3>
									<p class="post-desc">{post.data.description}</p>
								</div>
								<span class="post-meta">
									<FormattedDate date={post.data.pubDate} />
									{post.data.tags.map((tag) => (
										<span class="post-tag">{TAG_LABELS.en[tag]}</span>
									))}
								</span>
							</a>
						))
					}
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.home {
		width: 960px;
	}

	.intro {
		padding: 5rem 0 4rem;
		border-bottom: 1px solid rgb(var(--gray-light));
	}

	.eyebrow {
		margin: 0 0 0.85rem;
		color: var(--accent-dark);
		font-size: 0.88rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.lede {
		max-width: 680px;
		color: rgb(var(--gray-dark));
		font-size: 1.35rem;
		line-height: 1.55;
	}

	.actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 2rem;
	}

	.button {
		display: inline-flex;
		align-items: center;
		min-height: 44px;
		padding: 0 1.25rem;
		border: 1px solid rgb(var(--black));
		border-radius: 999px;
		background: rgb(var(--black));
		color: #fff;
		font-size: 0.95rem;
		font-weight: 700;
		text-decoration: none;
		transition: transform 160ms ease, background 160ms ease;
	}

	.button:hover {
		color: #fff;
		transform: translateY(-2px);
	}

	.button.secondary {
		background: transparent;
		color: rgb(var(--black));
	}

	.button.secondary:hover {
		color: rgb(var(--black));
		background: rgba(0, 0, 0, 0.04);
	}

	.section {
		padding: 3.5rem 0;
		border-bottom: 1px solid rgb(var(--gray-light));
	}

	.section:last-of-type {
		border-bottom: 0;
	}

	.section-heading {
		margin-bottom: 1.75rem;
	}

	.section-heading h2 {
		margin: 0;
		font-size: 1.6rem;
		letter-spacing: -0.01em;
	}

	.expandable-grid {
		display: grid;
		gap: 1rem;
	}

	.expand-card {
		border: 1px solid rgb(var(--gray-light));
		border-radius: 14px;
		background: #fff;
		overflow: hidden;
		transition: border-color 200ms ease, box-shadow 200ms ease;
	}

	.expand-card:hover {
		border-color: rgb(var(--gray));
	}

	.expand-card[open] {
		border-color: var(--accent);
		box-shadow: 0 6px 20px rgba(var(--black), 0.06);
	}

	.chevron {
		flex-shrink: 0;
		width: 9px;
		height: 9px;
		margin-top: 0.35rem;
		border-right: 2px solid rgb(var(--gray));
		border-bottom: 2px solid rgb(var(--gray));
		transform: rotate(45deg);
		transition: transform 200ms ease;
	}

	.expand-card[open] .chevron {
		border-color: var(--accent);
		transform: rotate(-135deg);
	}

	.expand-summary {
		cursor: pointer;
		padding: 1.25rem 1.5rem;
		list-style: none;
		user-select: none;
	}

	.expand-summary::-webkit-details-marker {
		display: none;
	}

	.summary-header {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 1rem;
	}

	.card-title {
		margin: 0;
		color: rgb(var(--black));
		font-size: 1.15rem;
		font-weight: 700;
		line-height: 1.4;
	}

	.card-tagline {
		margin: 0.5rem 0 0.85rem;
		color: rgb(var(--gray-dark));
		font-size: 1.05rem;
		line-height: 1.5;
	}

	.tag-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4rem;
	}

	.tech-tag {
		padding: 0.15rem 0.6rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 999px;
		color: rgb(var(--gray));
		font-size: 0.85rem;
		font-weight: 600;
		letter-spacing: 0.01em;
	}

	.private-tag {
		border-style: dashed;
	}

	.card-actions[hidden] {
		display: none;
	}

	.expand-content {
		padding: 0 1.5rem 1.5rem;
		border-top: 1px dashed rgb(var(--gray-light));
		margin-top: 0.5rem;
		padding-top: 1.25rem;
	}

	.content-block h4 {
		margin: 0 0 0.75rem;
		color: rgb(var(--black));
		font-size: 1rem;
	}

	.content-block ul {
		margin: 0;
		padding-left: 1.2rem;
		color: rgb(var(--gray-dark));
		font-size: 1rem;
		line-height: 1.6;
	}

	.content-block li {
		margin-bottom: 0.4rem;
	}

	.card-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
		margin-top: 1.25rem;
	}

	.action-btn {
		display: inline-flex;
		align-items: center;
		padding: 0.4rem 0.85rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 6px;
		background: #fff;
		color: rgb(var(--black));
		font-size: 0.92rem;
		font-weight: 600;
		text-decoration: none;
		transition: all 160ms ease;
	}

	.action-btn:hover {
		border-color: var(--accent);
		color: var(--accent-dark);
		background: rgba(var(--gray-light), 0.5);
	}

	.post-list {
		display: grid;
		border-top: 1px solid rgb(var(--gray-light));
	}

	.post-row {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 2rem;
		padding: 1.15rem 0.5rem;
		border-bottom: 1px solid rgb(var(--gray-light));
		color: inherit;
		text-decoration: none;
		transition: background 160ms ease, padding-left 160ms ease;
	}

	.post-row:hover {
		background: rgba(var(--gray-light), 0.45);
		padding-left: 0.9rem;
	}

	.post-title {
		margin: 0 0 0.25rem;
		color: rgb(var(--black));
		font-size: 1.08rem;
		line-height: 1.45;
	}

	.post-row:hover .post-title {
		color: var(--accent);
	}

	.post-desc {
		margin: 0;
		color: rgb(var(--gray));
		font-size: 0.95rem;
		line-height: 1.55;
	}

	.post-meta {
		display: flex;
		flex-direction: column;
		align-items: flex-end;
		gap: 0.3rem;
		flex-shrink: 0;
		color: rgb(var(--gray));
		font-size: 0.9rem;
		font-variant-numeric: tabular-nums;
	}

	.post-tag {
		padding: 0.05rem 0.5rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 999px;
		font-size: 0.85rem;
		font-weight: 600;
		white-space: nowrap;
	}

	@media (max-width: 720px) {
		.intro {
			padding: 3rem 0 2.5rem;
		}

		.lede {
			font-size: 1.1rem;
		}

		.post-row {
			flex-direction: column;
			gap: 0.4rem;
		}

		.post-meta {
			flex-direction: row;
			align-items: center;
			gap: 0.6rem;
		}
	}
</style>
